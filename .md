### 🔷 1️⃣ tkinter MODULE (`import tkinter as tk`)

### What is `tkinter`?

`tkinter` is Python’s **built-in GUI library** used to create **windows, buttons, labels, inputs, tables**, etc.

---

### 🔹 tk.Tk()

### 📌 Syntax

```python
root = tk.Tk()
```

### 📌 Why we use it

* Creates the **main application window**
* Without this, **no GUI can exist**

### 📌 Example

```python
import tkinter as tk

root = tk.Tk()
root.title("My First App")
root.geometry("300x200")
root.mainloop()
```

### 📌 What it makes

A **main window**

### 📌 Output (visual idea)

```
┌────────────────────────┐
│   My First App         │
│                        │
│                        │
│                        │
└────────────────────────┘
```

---

## 🔹 tk.Toplevel()

### 📌 Syntax

```python
win = tk.Toplevel(parent)
```

### 📌 Why we use it

* Opens **another window**
* Used for **dashboard, popup, add form, settings**

### 📌 Example

```python
win = tk.Toplevel(root)
win.title("Second Window")
```

### 📌 Output

```
  Main Window        Second Window
┌─────────────┐     ┌─────────────┐
│             │     │             │
│             │     │             │
│             │     │             │
└─────────────┘     └─────────────┘
```

---

### 🔹 tk.Label()

### 📌 Syntax

```python
tk.Label(parent, text="", font=(), fg="")
```

### 📌 Why we use it

* To **show text**
* Titles, headings, instructions

### 📌 Example

```python
tk.Label(root, text="Welcome", font=("Arial", 16, "bold")
        ,fg="green").pack()
```

### 📌 Output

```
Welcome
```

---

### 🔹 tk.Entry()

### 📌 Syntax

```python
tk.Entry(parent, show="")
```

### 📌 Why we use it

* Takes **user input**
* Username, password, amount

### 📌 Example

```python
tk.Entry(root).pack()
```

### 📌 Password Example

```python
tk.Entry(root, show="*").pack()
```

### 📌 Output

```
┌───────────────┐
│ _____________ │ 
└───────────────┘
```
* So that User can Enter the text here.

---

### 🔹 tk.Button()

### 📌 Syntax

```python
tk.Button(parent, text="", command=function)
```

### 📌 Why we use it

* To perform an **action**
* Calls a function when clicked

### 📌 Example

```python
def say_hi():
    print("Hi")

tk.Button(root, text="Click Me", command=say_hi).pack()
```

### 📌 Output

```
┌───────────┐
│ Click Me  │ 
└───────────┘
```

---

### 🔹 tk.Frame()

### 📌 Syntax

```python
tk.Frame(parent)
```

### 📌 Why we use it

* Groups widgets together
* Helps layout organization

### 📌 Example

```python
frame = tk.Frame(root)
frame.pack()

tk.Label(frame, text="Inside Frame").pack()
```

### 📌 Output

```
┌────────────┐
│ Inside     │
│ Frame      │
└────────────┘
```

---

### 🔹 .pack()

### 📌 Syntax

```python
widget.pack(pady=10)
```

### 📌 Why we use it

* Simple layout manager
* Places widgets **top to bottom**

### 📌 Example

```python
tk.Label(root, text="A").pack()
tk.Label(root, text="B").pack()
```

### 📌 Output

```
A
B
```

---

### 🔹 .grid()

### 📌 Syntax

```python
widget.grid(row=0, column=0)
```

### 📌 Why we use it

* Table-like layout
* Used for **forms**

### 📌 Example

```python
tk.Label(root, text="Name").grid(row=0, column=0)
tk.Entry(root).grid(row=0, column=1)
```

### 📌 Output

```
       ┌───────────────┐
Name | │ _____________ │ 
       └───────────────┘
```
* So here User can enter the Username here.

---

### 🔹 .destroy()

### 📌 Syntax

```python
window.destroy()
```

### 📌 Why we use it

* Closes a window

### 📌 Example

```python
tk.Button(root, text="Close", command=root.destroy).pack()
```

---

### 🔹 .withdraw()

### 📌 Syntax

```python
root.withdraw()
```

### 📌 Why we use it

* Hides window (login → dashboard)

---

### 🔹 .deiconify()

### 📌 Syntax

```python
root.deiconify()
```

### 📌 Why we use it

* Shows hidden window again


# 🔷 2️⃣ `tkinter.messagebox`

### What is `messagebox`?

`messagebox` is used to show **popup dialog boxes** to the user.
These are **not inside the main window**, they pop up on top.

Used for:

* Errors ❌
* Information ℹ️
* Warnings ⚠️
* Success messages ✅

You imported it like this:

```python
from tkinter import messagebox
```

---

### 🔹 `messagebox.showerror(title, message)`

### 🔸 Why we use it

* To tell the user **something went wrong**
* Example: wrong password, invalid input, DB error

---

### 🔸 Syntax

```python
messagebox.showerror("Title", "Message text")
```

---

### 🔸 Example Code

```python
import tkinter as tk
from tkinter import messagebox

root = tk.Tk()
root.geometry("300x200")

def show_err():
    messagebox.showerror("Login Error", "Wrong password")

tk.Button(root, text="Login", command=show_err).pack(pady=50)

root.mainloop()
```

---

### 🔸 Explanation (line by line)

```python
from tkinter import messagebox
```

👉 Imports popup dialog functions

```python
def show_err():
```

👉 Function that will be called on button click

```python
messagebox.showerror("Login Error", "Wrong password")
```

👉 Shows **error popup** with title + message

---

### 🔸 Output (visual idea)

```
┌──────────────────────────┐
│ ❌ Login Error           │
│ Wrong password           │
│        [ OK ]            │
└──────────────────────────┘
```

---

### 🔹 `messagebox.showinfo(title, message)`

### 🔸 Why we use it

* To give **information or success message**
* Example: data saved, login success

---

### 🔸 Syntax

```python
messagebox.showinfo("Title", "Message")
```

---

### 🔸 Example Code

```python
def saved():
    messagebox.showinfo("Success", "Data saved successfully")
```

---

### 🔸 Output

```
┌──────────────────────────┐
│ ℹ Success                │
│  Data saved successfully │
│          [OK]            │
└──────────────────────────┘
```

---

### 🔷 3️⃣ `tkinter.ttk` (Themed Tkinter)

### What is `ttk`?

`ttk` provides **modern-looking widgets** (better than classic tkinter).

You imported it like:

```python
from tkinter import ttk
```

---

### 🔹 `ttk.Combobox()`

### 🔸 Why we use it

* Dropdown list
* User selects **one option**
* Prevents typing wrong values

---

### 🔸 Syntax

```python
ttk.Combobox(parent, values=[...], state="readonly")
```

---

### 🔸 Example Code

```python
cb = ttk.Combobox(
    root,
    values=["Food", "Transport", "Rent"],
    state="readonly"
)
cb.pack(pady=10)
```

---

### 🔸 Explanation

```python
values=[...]
```

👉 List of selectable items

```python
state="readonly"
```

👉 User **cannot type**, only select

---

### 🔸 Output

```
[ Food ▼ ]
```

---

### 🔹 `ttk.Treeview()`

### 🔸 Why we use it

* Displays **table data**
* Used for transactions, history, reports

---

### 🔸 Syntax

```python
ttk.Treeview(parent, columns=(...), show="headings", height=...)
```

---

### 🔸 Example Code

```python
tree = ttk.Treeview(
    root,
    columns=("Amount", "Category"),
    show="headings",
    height=5
)

tree.heading("Amount", text="Amount")
tree.heading("Category", text="Category")

tree.pack()
```

---

### 🔸 Explanation

```python
columns=("Amount", "Category")
```

👉 Defines column names

```python
show="headings"
```

👉 Hides default empty column

```python
height=5
```

👉 Shows 5 rows at a time

---

### 🔸 Output

```
Amount | Category
-----------------
100    | Food
```

---

### 🔹 `.heading()`

### Syntax

```python
tree.heading(col, text="Title")
```

### Why

* Sets column header name

---

### 🔹 `.column()`

### Syntax

```python
tree.column(col, width=100, anchor="center")
```

### Why

* Controls width & alignment

---

### 🔹 `.insert()`

### Syntax

```python
tree.insert("", "end", values=(100, "Food"))
```

### Why

* Adds a new row

---

### 🔷 4️⃣ `decimal` MODULE

### Why NOT float?

```python
0.1 + 0.2 = 0.3000000004 ❌
```

### Why Decimal?

* **Accurate money calculation**
* No rounding errors

---

### 🔹 `Decimal()`

### Syntax

```python
Decimal("value")
```

---

### Example Code

```python
from decimal import Decimal

income = Decimal("5000")
expense = Decimal("1234.50")

balance = income - expense
print(balance)
```

---

### Output

```
3765.50
```

---

### 🔹 `InvalidOperation`

### Why?

* Handles wrong input like `"abc"`

---

### Example

```python
from decimal import Decimal, InvalidOperation

try:
    x = Decimal("abc")
except InvalidOperation:
    print("Invalid number")
```

---
### 🔷 5️⃣ `datetime` MODULE

---

### 🔹 `date.today()`

### Syntax

```python
date.today()
```

---

### Example Code

```python
from datetime import date

today = date.today()
print(today)
```

---

### Output

```
2025-12-17
```

---

### 🔹 `.year`, `.month`, `.day`

```python
today.year   # 2025
today.month  # 12
today.day    # 17
```

---

# 🔷 6️⃣ `calendar` MODULE

---

### 🔹 `calendar.monthrange()`

### Syntax

```python
calendar.monthrange(year, month)
```

---

### Example

```python
import calendar

calendar.monthrange(2025, 2)
```

---

### Output

```
(5, 28)
```

👉 February 2025 has **28 days**

---

### 🔹 Last day checker (your function)

```python
def is_last_day_of_month(d):
    return d.day == calendar.monthrange(d.year, d.month)[1]
```

👉 Compares today’s day with **last day number**

---

### 🔷 7️⃣ `mysql.connector`

### Why?

* Store data permanently
* Used for **income, expense, goals**

---

### 🔹 `connect()`

### Syntax

```python
mysql.connector.connect(host, user, password, database)
```

---

### Example

```python
db = mysql.connector.connect(
    host="localhost",
    user="root",
    password="1234",
    database="fingo"
)
```

---

### 🔹 `.cursor()`

```python
cur = db.cursor()
```

👉 Cursor runs SQL commands

---

### 🔹 `.execute()`

```python
cur.execute("SELECT * FROM expenses")
```

---

### 🔹 `.fetchone()` / `.fetchall()`

```python
cur.fetchone()
cur.fetchall()
```

---

### 🔹 `.commit()`

```python
db.commit()
```

👉 Saves changes permanently

---

### 🔷 8️⃣ `matplotlib.pyplot`

### Why?

* Show **charts**
* Visual understanding of expenses

---

### 🔹 `plt.subplots()`

### Syntax

```python
fig, ax = plt.subplots(figsize=(5,5))
```

---

### 🔹 `ax.pie()`

### Example Code

```python
import matplotlib.pyplot as plt

sizes = [40, 60]
labels = ["Food", "Rent"]

fig, ax = plt.subplots(figsize=(5,5))
ax.pie(sizes, labels=labels, autopct="%1.1f%%", startangle=90)
ax.set_title("Expense Split")

plt.show()
```

---

### Explanation

```python
sizes
```

👉 Percentage values

```python
autopct="%1.1f%%"
```

👉 Shows percentage text

```python
startangle=90
```

👉 Rotates chart nicely

---

### Output

```
     🟢 Food 40%
     🔵 Rent 60%
   (Pie Chart)
```

* 🔹 `plt.show()`
👉 Displays the chart window

---

